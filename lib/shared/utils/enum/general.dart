enum AlertType { confirm, info, success, failed }
enum ManageType { create, edit }
enum ActionPushType { manage, choose }
enum ConfirmationType { register, forgetpassword }
enum RoleGroupType { superAdmin, admin, user }
